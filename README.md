# Python App

Sample Python application

## Set Up Dev Environment

* Create a venv:

    ```bash
    uv venv -p 3.13.0 --seed
    ```

* Install dependencies:

    ```bash
    pip install -r requirements.txt
    ```
